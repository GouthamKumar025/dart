void main() {
  // replacing the whitespaces
  String name = "The man is sitting near the river";
  print(name.replaceAll(' ', ''));

  //converting the string to int
  String ans = "342";
  print(int.parse(ans));
}
